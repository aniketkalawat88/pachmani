/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.pachmarhiayurveda.com', 'res.cloudinary.com','plus.unsplash.com','randomuser.me'],
    }
};

export default nextConfig;
